# webhook-server-1 environment variables

PORT=5002

DATABASE_URL="postgresql://postgres:postgres@postgres-webhook:5432/webhook_server_db?schema=public"

# Comma-separated list of origins allowed to call this API (the webhook frontend)
CORS_ORIGIN="http://localhost:5001"
